framework:
#    default_locale: '%locale%'
#    translator:
#        paths:
#            - '%kernel.project_dir%/translations'
#        fallbacks:
#            - '%locale%'

    default_locale: 'ru'
    translator:
        paths:
            - '%kernel.project_dir%/translations'
        fallbacks:
            - 'ru'